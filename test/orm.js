'use strict';

const assert = require('assert');
const lala = require('../index');
/*
describe('ORM', async () => {
    let element = null;
    it('Create object from entity.', () => {
        element = lala.Peke.entity('test');
    });
    await it('Insert some fields.', async () => {
        await element.insert({
            name: 'Sig.',
            surname: 'Test'
        });
    });
    await it('Create an user.', async () => {
        let user = new lala.User();
        user.setName('Sig.').setSurname('Test').save();
    });
    await it('Retrieving an user.', async () => {
        //let user = new lala.User.find();
    });
});
*/
