'use strict';

const assert = require('assert');
const filesystem = require('fs');
const lala = require('../..');
const {
    fetchHTTPResponse,
    attachBasicRoutes
} = require('../utilities');

describe('', () => {

});